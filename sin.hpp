/*#ifndef SHORTCIRCUIT_SIN_HPP
#define SHORTCIRCUIT_SIN_HPP

#include <iostream>
#include <cmath>
#include "Component.hpp"
using namespace std;
/*
class SIN:public Component  {
private:
    double dc_offset;
    double ac_amplitude;
    double frequency;
public:
    // set initial value
    SIN(string name,string pin1,string pin2,double DC, double amplitude, double set_frequency ){
        _identifier = name;
        _n1 = convert_node_to_int(n1);
        _n2 = convert_node_to_int(n2);
        _value = DC;
        dc_offset = DC;
        ac_amplitude = amplitude;
        frequency = set_frequency;
    }
    double get_real_voltage(double time);
};
*/

#endif //SHORTCIRCUIT_SIN_HPP
/*